<h4>Best games:</h4>

<ul class="game-list">
  <% @games.each do |game| %>
    <li class="game-list-item"><a href="/games/<%= game.id %>"><%= game.name %></a> <%= game.rating %></li>
  <% end %>
</ul>
